# Extension workspaces

Extension packages live here as separate npm workspaces. The first extension package is `packages/extensions/herdr`, published as `@piewf/herdr`. The existing `packages/core/examples/workflow-extension-template` remains the general extension reference.
